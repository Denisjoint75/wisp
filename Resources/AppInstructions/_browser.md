## Browser Computer Use

Open a new tab (cmd+t, or `wisp chrome new URL` for DevTools tabs) when you move to a different site or start an unrelated task. Stay in the current tab only if the user asked to continue there, or the page you are on is obviously the next step of the same workflow. This keeps the user's own pages intact.
